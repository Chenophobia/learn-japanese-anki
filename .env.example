# Directory holding app.db (bind-mounted in Docker)
DATA_DIR=./data
# Reserved, not currently read by the app. Session ids are 32-byte CSPRNG
# tokens looked up server-side (not HMAC-signed), so no secret is needed.
# See README "Environment variables" for details.
SESSION_SECRET=changeme

services:
  app:
    build: .
    container_name: learn-japanese
    ports:
      - '127.0.0.1:3001:3001'
    env_file: .env
    environment:
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
